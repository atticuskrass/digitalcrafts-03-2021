CREATE DATABASE artistlist;

CREATE TABLE artist(
    artist_id SERIAL PRIMARY KEY,
    first_name VARCHAR(30);
    last_name VARCHAR(30);
    country_name VARCHAR(30);
    city_name VARCHAR(30);
    );
